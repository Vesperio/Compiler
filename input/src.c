// #include <stdio.h>

int main() {
    printf("Hello World!\n");
    int a = 10, b = 20; // 无符号整数的识别
    int _sum = a + b; // _符号打头的标识符的识别、算术运算符的识别

    if (_sum > b) // 保留字的识别
        printf("_sum 大于 b")

    string 201900800559_name = "殷东鹏" // 非法字符的报错 -> 未完成!

    return 0; // 保留字的识别
}